computer_parts = ["computer", "monitor", "keyboard", "mouse", "mouse mat"]

for part in computer_parts:
    print(part)

print()
print(computer_parts[2])

print(computer_parts[0:3])
print(computer_parts[3:5])
print(computer_parts[-1]) # It always gives the last item of the list
print(computer_parts[-5])

# Strings are immutable, which means they can't be changed.
# Lists, on the other hand, are mutable. You can change the contents of a list.
